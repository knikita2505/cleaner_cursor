# feature_flags

## Назначение
Скрытый технический экран для разработчиков и QA.  
Не отображается в обычной навигации.  
Открывается только через *секретную фразу*, что исключает случайный доступ пользователем.

Экран позволяет:
- переключать экспериментальные фичи,
- тестировать A/B варианты paywall и онбординга,
- сбрасывать состояния приложения,
- симулировать разные условия устройства,
- имитировать подписки,
- управлять логированием,
- включать/выключать тяжелые модули (Battery, Health, Email, Highlights).

---

# 🔐 МЕХАНИЗМ АКТИВАЦИИ (SECRET PASSPHRASE)

## Hidden Trigger
Экран активируется через **секретную фразу**, которую можно ввести только в одном месте:

### Механика:
1. Пользователь открывает:  
   **Settings → About App** (экран с информацией о версии)
2. На этом экране есть скрытая зона:
   - обычный текст *“App Version: X.X”* остаётся неизменным
   - при **длинном тапе (2 сек)** появляется тонкое текстовое поле (невидимое по умолчанию)
3. Пользователь вводит **секретную фразу**:
4. После ввода → кнопка “OK” →  
→ открывается экран Feature Flags.

### Особенности:
- Поле ввода исчезает, если фраза неверная.
- При вводе правильной фразы:
- короткая вибрация,
- лёгкая вспышка/blur,
- мгновенный переход в Developer Panel.

### Защита:
- поле ввода скрыто (0 opacity) до long press,
- пока пользователь не знает про фразу →  
**невозможно попасть**,  
- обычный пользователь *ничего не увидит* и не сможет догадаться.

---

# UI ЭКРАНА FEATURE FLAGS

## Header
- Title: **Developer Panel**
- Subtitle: `Use responsibly`

---

# SECTION 1 — FEATURE TOGGLES

Каждый элемент — Toggle + описание.

### ✔ Highlights Detector
Включает/отключает модуль автоматического выбора лучших фото.

### ✔ Battery Module
Активация:
- battery animation  
- battery tips  
- battery dashboard

### ✔ Device Health
Включает:
- health score  
- uptime мониторинг  
- системные рекомендации  

### ✔ System Tips
Показывает раздел рекомендаций устройства.

### ✔ Email Cleaner
Отключает/включает модуль почты.

### ✔ Secret Folder
Включает шифрованное хранилище.

### ✔ Swipe Clean
Включает свайповый интерфейс чистки.

### ✔ Cleaning History
Включает/отключает историю чисток.

---

# SECTION 2 — A/B TESTING

### Paywall Variant  
**A / B / Auto**

### Onboarding Variant  
**A / B / Auto**

### Notifications Strategy  
**Daily / Weekly / Off**

---

# SECTION 3 — DEBUG TOOLS

### 🔄 Reset Onboarding
Сбрасывает флаг `hasCompletedOnboarding`.

### 🔄 Reset Permissions
Сбрасывает все локальные флаги разрешений.

### 🔄 Reset Free Limit
Устанавливает free-limit = 0  
(Полезно для повторной демонстрации paywall-limit).

### 🔄 Reset Caches
Стирает:
- photo scan cache  
- video scan cache  
- cleaning history  
- highlights cache  

### 🔄 Force First Launch
Сбрасывает состояние приложения как при первой установке.

---

# SECTION 4 — SIMULATE STATES

### Storage 90%
Имитирует состояние устройства: хранилище заполнено на 90%.

### Battery Low
Имитирует заряд <20%.

### Overheating
Имитирует температуру устройства выше нормы.

### Highlights Ready
Имитирует “готовы новые лучшие фото”.

### Free Limit Reached
Имитирует достижение бесплатного лимита.

---

# SECTION 5 — SUBSCRIPTION CONTROL

### Set Premium = ON  
Принудительно включает подписку.

### Set Premium = OFF  
Убирает подписку.

### Activate Trial  
Имитирует активный триал.

### Show Paywall Now  
Мгновенно вызывает Paywall A или B (в зависимости от выбранного варианта).

---

# SECTION 6 — LOGGING / ANALYTICS

### Verbose Logging
Включает подробные логи для Photo/Video сервисов.

### Firebase Debug Mode
Переводит Firebase в debug-режим.

### Show Debug Console
Открывает внутренний экран со списком последних аналитических событий.

---

# UI СТИЛЬ
- Фон: #111214  
- Cards: #15161B  
- Текст: #EEE 87% opacity  
- Иконки: gradient cyan→purple  
- Важные элементы: красные предупреждения, фиолетовые toggles  
- Кнопки: минималистичные, как в Settings  
- Поля ввода (секретная фраза): скрытое, появляется на отдельном экране

---

# ЛОГИКА ПАНЕЛИ

- Все флаги хранятся в UserDefaults.  
- Некоторые флаги влияют на работу сервисов в рантайме (без перезапуска приложения).  
- Применение фич происходит мгновенно.  
- Состояния должны сохраняться после перезапуска приложения.  
- Любое изменение → логировать в Firebase как `dev_flag_changed`.

---

# EDGE CASES

- При запуске релизной (App Store) сборки можно добавить конфиг:
- либо полностью отключить панель,
- либо оставить её только через секретный код.
- При вводе неправильной фразы → показать unobtrusive toast:
- “Invalid developer passphrase”.

- Если поле ввода неактивно → оно само исчезает через 10 секунд бездействия.

---

# Связанные задачи
- FF-001 — Global Feature Toggles  
- FF-002 — A/B Remote config  
- SETTINGS-001 — Settings  
- PAYWALL-004 — A/B Switching Logic